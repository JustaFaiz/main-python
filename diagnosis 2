import openpyxl

wb1 = openpyxl.load_workbook(r'file1.xlsx', data_only=True)
ws1 = wb1['Sheet1']

# print first 5 rows so we can see where headers actually are
for row_num in range(1, 6):
    row_vals = [cell.value for cell in ws1[row_num]]
    print(f"Row {row_num}: {row_vals}")
